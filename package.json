{
  "name": "ai-vision-voice-assistant",
  "private": true,
  "workspaces": [
    "frontend",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w server\" \"npm run dev -w frontend\"",
    "typecheck": "npm run typecheck -w server && npm run typecheck -w frontend",
    "build": "npm run build -w server && npm run build -w frontend"
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
